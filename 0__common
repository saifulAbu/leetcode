require 'pry'
require 'pry-nav'

binding.pry

## os struct
require "ostruct"
australia = OpenStruct.new(:country => "Australia", :capital => "Canberra")

begin

end while condition

# hashmap
map = Hash.new() {|k| []}

## set
require 'set'
s = Set.new