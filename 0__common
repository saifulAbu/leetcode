## debugging syntax
require 'pry'
require 'pry-nav'
binding.pry

## ruby syntax
# multi dimensinoal array
arr1 = Array.new(3) { Array.new(3)}

# array with default value
arr1 = Array.new(3, false)


# os struct
require "ostruct"
australia = OpenStruct.new(:country => "Australia", :capital => "Canberra")

# loop
begin

end while condition

for i in (0..10) do
end

for i in [1, 2, 3] do
end

# hashmap
map = Hash.new() {|k| []}

## set
require 'set'
s = Set.new

# queue
queue = Queue.new
queue.enq
queue.deq
queue.empty?
queue.size

#sort
sorted = arr.sort